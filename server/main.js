import { Meteor } from 'meteor/meteor';
import '../imports/api/elements/elements.js';
import '../imports/api/measures/measures.js';
import '../imports/api/reports/reports.js';

Meteor.startup(() => {
  // code to run on server at startup
});
